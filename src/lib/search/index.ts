// Public search API. UI imports from here; the engine stays swappable.
export { searchAll } from './search';
export { buildTagsByTarget } from './dataset';
export { highlightSegments } from './highlight';
