{
	"name": "copynotes-mcp",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "vitest run",
		"build:flat": "rm -rf node_modules && pnpm install --config.node-linker=hoisted --prod"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.29.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"vitest": "^4.1.10"
	}
}
